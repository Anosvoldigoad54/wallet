[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
# Vite's preview server serves the production build.
# Railway injects $PORT automatically.
cmd = "npx vite preview --host 0.0.0.0 --port $PORT"
